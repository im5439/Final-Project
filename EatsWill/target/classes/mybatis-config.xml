<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE configuration
PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>
<!-- 다른 프레임워크와 같이 사용할때 다른 스프링워크는 DB인식을 못할수있어서 써줌.  -->
<properties>
	<property name="driver" value="oracle.jdbc.driver.OracleDriver"/>
	<property name="url" value="jdbc:oracle:thin:@192.168.16.18:1521:TestDB"/>
	<property name="username" value="hye"/>
	<property name="password" value="a123"/>
</properties>

<environments default="development">
	<environment id="development">
		<transactionManager type="JDBC"/>
		<dataSource type="POOLED">
			<property name="driver" value="${driver}"/>
			<property name="url" value="${url}"/>
			<property name="username" value="${username}"/>
			<property name="password" value="${password}"/>
		</dataSource>
	</environment>
</environments>

<!-- spring에서는 위치 mapper만 있어도됨.  -->
<mappers>
	<mapper resource="com/eatswill/mybatis/menuListMapper.xml"/>
</mappers>

</configuration>